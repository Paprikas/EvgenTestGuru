<header>
<%= link_to 'Test Guru', root_path%>

<% if user_signed_in? %>
  <p class="nav user">Welcome, <b><%= current_user.name %></b> Guru </p>
  <p class="nav user"> <%= link_to 'Log out', destroy_user_session_path, data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %></p>
<% else %>
  <p class="nav user">
  <%= link_to 'Sign up', new_user_registration_path %>
  <%= link_to 'Log in', new_user_session_path %>
<% end %>
</p>



</header>